#include "magicianlib.h"
#include <bits/stdc++.h>
using namespace std;

vector<int> solveAlice(const vector<vector<int>> &adj){
	return vector<int>({7, 3, 11, 5, 2});
}

vector<vector<int>> solveBob(const vector<int> &S){
	return vector<vector<int>>({
		vector<int>({}),
		vector<int>({}),
		vector<int>({1,4,}),
		vector<int>({2,0,}),
		vector<int>({})
	});
}
